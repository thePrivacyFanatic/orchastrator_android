name: orchastrator
description: An app aiming to help coordinate people.
publish_to: none
version: 1.0.0+1
environment:
  sdk: ">=3.2.0"
dependencies:
  flutter:
    sdk: flutter
  provider:
  shared_preferences:
  path:
  path_provider:
  collection:
  string_validator:
  smart_stepper:
  mobile_scanner:
  async: ^2.13.0
  flutter_eval: ^0.8.2
  dart_eval: ^0.8.4
  eval_annotation: ^0.8.1
  aes_crypt_null_safe: ^3.1.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
